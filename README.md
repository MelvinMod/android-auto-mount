Mounts a specific partition by checking if it's already mounted, going through readable disk images to find and attach the first valid one (unmounting failures), and if a super image exists under /userdata, it sets up a read-only loop device and makes device-mapper entries for dynamic partitions.
